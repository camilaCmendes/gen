import { styled } from "@mui/system";

export const LayoutContainer = styled("div")({
  display: "flex",
  flexDirection: "column",
});

export const Space = styled("div")(() => ({ marginBottom: "8rem" }));
